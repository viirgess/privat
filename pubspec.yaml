name: privat_test_task
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.7.0
  equatable: ^2.0.5
  freezed_annotation: ^2.4.4
  flutter_bloc: ^8.1.6
  flutter_dotenv: ^5.1.0
  get_it:
  injectable:
  path:
  path_provider: ^2.1.4
  sqflite: ^2.3.3+1
  url_launcher: ^6.3.0

dev_dependencies:
  build_runner:
  flutter_lints: ^3.0.0
  freezed: ^2.5.2
  injectable_generator:

flutter:
  uses-material-design: true
  assets:
    - .env
